<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">
    <!--회원가입-->
    <insert id="createUser">
      INSERT INTO tb_user (EMAIL, PASSWORD, NAME, CREATE_DATE, MODIFIED_DATE)
      VALUES (#{email}, #{password}, #{name}, now(), now())
    </insert>

    <!--회원 리스트-->
    <select id="list" resultType="com.cafe24.pickmetop.user.domain.User">
      SELECT
        ID AS id
        , EMAIL AS email
        , NAME AS name
        , CREATE_DATE AS createDate
        , MODIFIED_DATE AS modifiedDate
	  FROM tb_user
    </select>

    <!--회원 정보 상세조회-->
    <select id="show" resultType="com.cafe24.pickmetop.user.domain.User">
        SELECT
        ID AS id
        , EMAIL AS email
        , NAME AS name
        , CREATE_DATE AS createDate
        , MODIFIED_DATE AS modifiedDate
	  FROM tb_user
      WHERE ID = #{id}
    </select>

    <!--회원 정보 수정-->
    <update id="update" parameterType="com.cafe24.pickmetop.user.domain.User">
        UPDATE tb_user
        SET
            EMAIL = #{email}
            , NAME = #{name}
            , PASSWORD = #{password}
            , MODIFIED_DATE = NOW()
        WHERE
            ID = #{id}
    </update>

    <!--회원 삭제 / 탈퇴-->
    <delete id="delete" parameterType="Long">
        DELETE FROM tb_user WHERE ID = #{id}
    </delete>

    <!--비밀번호 확인-->
    <select id="checkPw" resultType="int">
        SELECT
			COUNT(*)
		FROM tb_user
		WHERE EMAIL = #{email} AND PASSWORD = #{password}
    </select>

    <!--로그인-->
    <select id="login" resultType="String">
        SELECT NAME FROM tb_user WHERE EMAIL = #{email} AND PASSWORD = #{password}
    </select>

    <!--로그인된 회원 정보조회-->
    <select id="loginUser" resultType="com.cafe24.pickmetop.user.domain.User">
        SELECT
            ID
          , EMAIL
          , NAME
          , CREATE_DATE
          , MODIFIED_DATE
        FROM tb_user
        WHERE EMAIL = #{email} AND PASSWORD = #{password}
    </select>
</mapper>
